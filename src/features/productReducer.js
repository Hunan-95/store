export const productReducer = (state = {}, { type, payload }) => {
  switch (type) {
    case "add":
      {
        return state;
      }
    //   case "ff":{

    //   }
      return state;
  }

  return state;
};



export const productDataReducer = [
    {
        id:1,
        name:"jeki"
    }
]